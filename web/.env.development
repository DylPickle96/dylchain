# `npm run dev` talks to a local dyld over HTTP (see vite.config.ts proxy).
# `npm run build` defaults to the in-page wasm backend, which needs no
# server, so the built site deploys as static files anywhere.
VITE_BACKEND=http
